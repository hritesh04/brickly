{
  "name": "@brickly/types",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsc",
    "dev": "npx tsc --watch --preserveWatchOutput",
    "prepare": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Shared TypeScript types for Brickly project",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
